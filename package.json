{
  "name": "getroundcraft",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "publish-pattern": "node scripts/add-pattern.mjs",
    "deploy": "astro build && git add -A && git commit -m \"Publish pattern updates\" && git push origin main"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource-variable/hanken-grotesk": "^5.3.0",
    "astro": "^5.6.0"
  }
}
